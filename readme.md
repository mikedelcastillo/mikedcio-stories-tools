the application should accomplish several things
- listen for telegram messages
- be the file manager
    - sync local files with local
    - be able to zip

sudo apt-get install pkg-config libssl-dev